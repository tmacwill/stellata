import setuptools

setuptools.setup(
    name='stellata',
    version='0.0.1',
    author='Tommy MacWilliam',
    packages=setuptools.find_packages(),
    install_requires=[
        'psycopg2',
    ]
)
